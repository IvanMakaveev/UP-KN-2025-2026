# За в час

## Указатели и низове 

**Задача 1:** Напишете функция `void swap(int*& first, int*& second)`, която разменя стойностите на два указателя, които сочат към целочислени числа.

**Пример:**

```c++
21 37
```

```c++
37 21
```

**Задача 2:** Напишете програма, която извежда елементите на масив без използването на оператор [].

Бонус: Да се направи функция `int at(const int* arr, unsigned int index)`, която приема указател към int масив и връща елемента на индекс index.(без []) 


**Пример:**

```c++
[ 0, 7, 3, 5, 4 ] 2
```

```c++
3
```

**Максималната дължина на стринговете във всички задачи е 1024.**


**Задача 3:** Да се напише функция, която приема стринг и 2 цели числа **beg** и **end** и връща подстринг на въведения от индекс **beg** до индекс **end**. Ако **end** е повече от дължината на стринга, връщаме стринга до края. Ако **beg** > дължината, връщаме празен стринг.<br />

Вход: 
```c++
Hello 1 3
```
Изход: 
```c++
ell
```
Вход: 
```c++
Hello 1 6
```
Изход: 
```c++
ello
```

Вход:
```c++
Hello 5 6
```
Изход:
```c++
""
```

**Задача 4:** Да се напише функция, която приема стринг с произволна дължина от цифри, и стринг **result**. Функцията да обработва **result** така, че да представлява стринг, в който пише коя цифра колко пъти се среща.

**Пример:**

Вход:
```c++
4231148
```
Изход:
```c++
2x1,1x2,1x3,2x4,1x8
```

**Задача 5:** Да се напише функция, която приема стринг (съставен само от малки букви) и изтрива всички повтарящи се букви в него.<br />

Вход: 
```c++
"cbacdcbc"
```
Изход: 
```c++
"cbad"
```

**Задача 6:** Напишете функция, която приема 3 стринга - text, where и what и замества в text всяко срещане на where с what.


*Да се реши първо с допълнтилена памет и след това без допълнителна памет (in-place).*

*Вход: "I am the best of the best", "best" , "worst" , Изход: "I am the worst of the worst"*


## Матрици

**Задача 1:** Да се напише функция, която приема матрица и връща най-малкия й елемент.

**Пример:**

Вход:
```c++
3 5 1 
6 2 4
9 0 8
```
Изход:
```c++
0
```

**Задача 2:** Да се напише функция, която приема квадратна матрица с **n** на **n** елемента отпечатва главния диагонал, след това и второстепенния.

**Пример:**

Вход:
```c++
1 2 3
4 5 6
7 8 9
```
Изход:
```c++
1 5 9
3 5 7
```

**Задача 3:** Да се напише функция, която по даден матрица отпечатва елементите ѝ на зиг-заг (първи ред отляво надясно, втори ред отдясно наляво и т.н.)

Вход:
```c++
1 2 3
4 5 6
7 8 9
```

Изход:
```c++
1 2 3 
6 5 4
7 8 9
```

**Задача 4:** Да се напише функция, която приема квадратна матрица с **n** на **n** елемента и връща дали матрицата е триъгълна, т.е. под главния диагонал има само нули.

**Пример:**

Вход:
```c++
5 1 5 
0 5 9
0 6 7
```
Изход:
```c++
0
```

Вход:
```c++
5 1 5 
0 5 9
0 0 7
```
Изход:
```c++
1
```
**Задача 5:** Да се напише функция, която транспонира квадратна матрица с **n** на **n** елемента.

**Пример:**

Вход:
```c++
1 2 3 
4 5 6 
7 8 9
```
Изход:
```c++
1 4 7
2 5 8
3 6 9
```

# За домашна
## Указатели и низове (продължение)

**Задача 7:**  Напишете функция, която проверява дали масив е конкатенация на два палиндрома. 

**Задача 8:** Да се напише функция `void sortCharacters(const char* str, char* result)`. Нека функцията подрежда всички букви в низа във възходящ ред. Ако низа съдържа други символи(различни от букви) ги игнорирайте.

**Пример:**

```c++
"Programming123!"
```

```c++
"aggimmnoprr"
```

**Задача 9:** Напишете функция, която приема два стринга и връща true, ако те са анаграми (състоят се от същите букви в различен ред), и false в противен случай.

**Пример:**

Вход: 
```c++
"listen", "silent"
```
Изход: 
```c++
1
```

**Задача 10:** Да се напише функция, която приема 2 стринга и проверява колко пъти единия се съдържа в другия.<br />

**Пример:**

Вход: 
```c++
"abcdabcabababc", "abc"
```
Изход: 
```c++
3
```

**Задача 11:** Да се напише функция, която приема 2 стринга - **text** и **substring** и замества всяко срещане на текст равен на **substring** в **text** с толкова звездички, колкото е дължината на  **substring** (ако се игнорира разликата между главни и малко букви).<br />

**Пример:**

Вход: 
```c++
"Howdy! How are you? How was your day?"
"how"
```
Изход:
```c++
"***dy! *** are you? *** was your day?"
```

**Задача 12:** Напишете функция, която приема стринг и връща броя на всички различни подстрингове, които са палиндроми.

**Пример:**

Вход: 
```c++
"ababa"
```
Изход:
```c++
9
//(подстрингове: "a","b","a","b","a","aba","bab","aba","ababa")
```



